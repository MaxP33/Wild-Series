{% extends 'base.html.twig' %}

{% block title %}{{ 'season'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'season'|trans }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{ 'id'|trans }}</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>{{ 'season.year'|trans }}</th>
                <td>{{ season.year }}</td>
            </tr>
            <tr>
                <th>{{ 'season.description'|trans }}</th>
                <td>{{ season.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('season_index') }}">{{ 'season.list'|trans }}</a>

    <a href="{{ path('season_edit', {'id': season.id}) }}">{{ 'edit'|trans }}</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
